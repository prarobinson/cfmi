Quick Start:

virutalenv cfmi
cd cfmi
. bin/activate
git clone http://github.com/nocko/cfmi.git
cd cfmi
source ./bin/activate
pip install -r requirements.txt

Copy setings.py.dist to settings.py and edit it to your needs

A development server can be started by running python cfmi in the
virutalenv directory


Happy Hacking
